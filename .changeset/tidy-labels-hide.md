---
'@slop-ui/vue': minor
---

Add the VisuallyHidden component for content that assistive technology reaches
but sighted users do not see. It renders a single configurable element, keeps
content in the accessibility tree, forwards native attributes for live regions
and naming relationships, and can reveal focusable content such as skip links
while focus is inside it.
